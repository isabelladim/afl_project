select 
cast(LOCAL_TIME) as timestamp) as local_time,
cast(hgoals as integer) as home_goals,
cast("YEAR" as integer) as season,
cast(round as integer) as round,
cast(complete as integer) as complete,
cast(venue as text) as venue,
cast(winner as text) as winner,
cast(updated as timestamp) as updated_at,
cast(roundname as text) as round_name,
cast(agoals as integer) as away_goals,  
cast("DATE" as date) as game_date,
cast(hteam as text) as home_team,
cast(timestr as text) as time_str,
cast("ID" as text) as game_id,
cast(abehinds as integer) as away_behinds,
cast(winnerteamid as text) as winner_team_id,
cast(is_final as integer) as is_final,
cast(ascore as integer) as away_score,
cast(hteamid as text) as home_team_id,
cast(ateam as text) as away_team,
cast(tz as text) as time_zone,
cast(hscore as integer) as home_score,
cast(unixtime as bigint) as unix_time,
cast(hbehinds as integer) as home_behinds,
cast(is_grand_final as integer) as is_grand_final,
cast(ateamid as text) as away_team_id

from {{ source('raw', 'games') }}
